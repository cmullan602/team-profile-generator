const { fstat } = require("fs");
const Game = require("./lib/Employee");

// Initialize a new Game object
const game = new Game();

// Start playing
game.play();
creatTeamMember(){
    const

}


function createNewHTML(fileName, date) {
    HTMLFormControlsCollection.log('createHTML is running');
    fs.writeFile(fileName, data (err) =>
    err ? console.log(err))
}